[ServiceEx]
; 設定執行程式的完整路徑 (注意：路徑中有空白時需加上雙引號包起來)
;ServiceExeFullPath = "C:\aria2c.exe" 
; 執行該程式時需加上的參數
options = "--enable-rpc=true --rpc-listen-all=true "
; 該程式是否會與桌面互動 (即代表該程式執行時若有任何對話框是否應顯示的意思)
desktop = false
; 啟動類型 (可設定的值有 Auto, Manual, Disabled 三種) (預設: Auto)
Start=Auto
; 是否安裝完成後立即啟動 (預設: false) 
StartNow=true
